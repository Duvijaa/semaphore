version: v1.0
name: Demo Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004

blocks:
  - name: "DevOps Setup"
    task:
      jobs:
        - name: Run Verification Scripts
          commands:
            - echo "Starting the DevOps Build Pipeline..."
            - echo "Running system verification tests..."
            - echo "Application deployed successfully!"
